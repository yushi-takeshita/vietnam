<% provide(:title, t(".パスワードをお忘れですか？")) %>

<div data-scope-path="password_resets/new">
  <div class="row justify-content-center">
    <div class="col-md-8">
      <div class="text-wrapper">
        <h1><%= t(".パスワードをお忘れですか？") %></h1>
        <p><%= t(".以下のフォームにご登録頂いていたメールアドレスを入力してください。") %><br>
          <%= t(".すぐにパスワード再設定のご案内メールを送信いたします。") %>
        </p>
      </div>
      <%= form_with scope: :password_reset, url: password_resets_path, local: true do |f| %>
      <div class="form-group">
        <%= f.label :email %>
        <%= f.email_field :email, class: "form-control" %>
      </div>
      <%= f.submit class: "btn btn-lg btn-block btn-primary " %>
      <% end %>
    </div>
  </div>
</div>